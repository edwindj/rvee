#' create an R wrapper from a v function.
#'
#' like `inline::cfunction`, but for `v`
#' @param sig `character`
#' @param body `character`
vfunction <- function(sig, body){
  stop("Not implemented", call. = F)
}
